import React from 'react';
import { history } from 'umi';

export default function () {
  
  // history.push('/testField');
  history.push('/dynamicPageTool');

  return (
    <div>
      首页
    </div>
  );
}
